# Act 3 — setup–payoff ledger (implementation-ready)

Status: FROZEN 2026-09-09 with the doctrine audit. Classification per Portable Briefing §5: PAID / INTENTIONAL-UNRESOLVED / UNPAID. Rows marked UNPAID are paid by the frozen design as stated in "Frozen resolution".

## Ledger

| # | SETUP (beat, channel) | PAYOFF (beat, channel / later act) | CLASS | WHAT THE PLAYER MUST HAVE SEEN | ABLATION | NOTE |
|---|---|---|---|---|---|---|
| 1 | Bridge / east direction — Act 2, James and Truman ("veniva da est") | A2 `m5_bridge`: "La rotta di James e la strada di Ronette sono la stessa strada." (`m5.b1.bridge.p03`) | **PAID** | James's Act 2 line naming the east road | Delete Act 2's line → A2's Cooper line becomes an assertion with no antecedent (still readable, loses the "same road" recognition beat). Neutralize A2 → Act 2's setup dangles as an unconfirmed lead, not fatally. | Solid, symmetric payoff. Direction itself is still only *told*, not walked — see Fact Ledger row 3/violation 4. |
| 2 | The ring — placed at centre, M5 (`m5_ring`, `m5_cmp_ring`) | Custody choice `m5_s1` (A13); later "Loggia echo," explicitly **deferred** by both report (§6 "DEFER the Loggia echo to the finale pass") and map (§1 row `s1`) | **INTENTIONAL-UNRESOLVED** | The centre/dust pages (E8A/E8B) and the comparison feedback | Delete the ring's placement fact → custody choice loses its stakes (an object nobody proved was placed is just an object). Neutralize custody (skip the choice) → the ring's "cannot yet read" status (report §4 HUMAN REVELATION) has nowhere to land in Act 3, but the deferral is declared, not accidental. | Correctly flagged deferred, not unpaid — carries no obligation before the finale pass. |
| 3 | The ticket / fire formula — Gerard's verse, Act 2 | Optional comparison `m5_cmp_ticket_e5`: "La stessa formula, detta in un letto e incisa in un vagone. Ricorrenza — non ancora identità." (`m5.obs.ticket_e5`) | **PAID** (optional) | Gerard's verse (Act 2, E5_POESIA) and the ticket text E7A | Delete Act 2's verse → the comparison node has nothing to pair with (M5.json invariant already guards: "M5 completabile senza E5 e senza rimandi pendenti" — graceful). Neutralize the comparison → Act 2's verse setup simply stays unconnected to Act 3 (an intentional non-mandatory link). | Explicitly optional and declared non-blocking; the note itself ("ricorrenza, non ancora identità") pre-empts overreach into identity, per bible's serial-rule rigor. |
| 4 | The stove | Report proposes payoff at `m6_interrogation_falsa`'s third-man detail ("Guardava la stufa come si guarda una persona," `m6.b5.falsa.p06`) | **UNPAID** | PROPOSED node A7 (`E_STUFA`) — does not exist in `M5.json` | Delete the (nonexistent) setup → nothing changes, because there is no setup today; the payoff line already fires unconditioned. Neutralize the payoff → falsa_sicurezza's third-man beat still lands as testimony alone, so the *branch* survives, but the report's intended "informed tactic choice" (§9 "Is Jacques earned? ... Without cards, Jacques is a name Truman supplies") does not. | This is a payoff arriving with no setup at all — the mirror image of "no evidence introduced at the climax" (bible §5 rule 6), one act early. Fix: build the PROPOSED `E_STUFA` node (A7) before the falsa branch fires in any playthrough order — since tactic choice happens after the traincar in the current topology, this is achievable without reordering. |
| 5 | The cards | Report proposes payoff at all three `m6_interrogation_*` branches (bank/dealing references) and at `m6_tactic`'s recall page and `m6_p5`'s strong-support pair (map §4 rows 1, 3) | **UNPAID** | PROPOSED node A8 (`E_CARTE`) — does not exist in `M5.json`; the recall page and the P5 optional pair described in map §4 also do not exist yet | Delete the (nonexistent) setup → same as row 4, no change today. Neutralize the payoff → Jacques's "il banco era mio" line (`m6.b5.falsa.p04`) still works as an unsupported boast; P5 loses its `support_strong` variant (falls back to plain `PRESENCE_SUPPORTED`), a real but survivable loss of texture. | Same class of gap as row 4. Report explicitly ties the tactic choice's fairness to this: without E_CARTE the tactic pick is "a blind guess" (§3.4). |
| 6 | Audrey's cameo and her "dal lato giusto del fiume" promise — `m6_audrey`: "Ci vediamo dal lato giusto del fiume." (`m6.b3.audrey.p02`) | Report proposes: one Truman line at the night report, a variant on `audrey_vista_oej` — "La ragazza del guardaroba è tornata a casa con la barca delle otto. L'ho accompagnata io." (report §9, quoted as PROPOSED); replaces classic `truman_wait4` | **UNPAID today** (report classes it explicitly: "never paid," report §1 line 42, "Hook" column) | `m6_audrey` node (optional, live) sets `audrey_vista_oej`; the flag exists but the map confirms (§1 row 30) `audrey_vista_oej` is currently read by **nothing** — `m6_return_night`/`m6_news` have no Audrey branch, and classic `truman_wait4` (unconditional, per report §1 line 18 and §10 table) fires regardless of whether the flag is set | Delete the setup (skip `m6_audrey`, it's optional) → nothing currently breaks, because nothing reads the flag either way — proving the payoff is genuinely missing, not conditionally present. Neutralize the (currently nonexistent) payoff → no change, confirming there's nothing to neutralize yet. | Textbook unpaid setup per bible §5's ablation test: removing the setup changes nothing about the (missing) payoff. Minimal repair below. |
| 7 | Jacques's arrest as a success — `m6_arrest`: fear/flight/injury sequence, `jacques_preso` | B9 `m6_news`: the arrest's meaning is retroactively soured ("il metodo vince, poi perde" per report §4); tactic-specific regret lines (`m6.b8.news.cooper_prova/pressione/falsa`) | **PAID** | The arrest sequence itself (`m6_arrest`, mandatory) and the chosen tactic's specific admission | Delete the arrest → the news of death has no "we had him, and it still wasn't enough" charge; becomes a flat tragedy instead of a reversal. Neutralize the regret lines (generic instead of tactic-specific) → the payoff still lands but loses the causal specificity the bible calls for ("changed return," not a neutral one) — this would demote it toward cosmetic. | Currently correctly tactic-specific (three distinct Cooper lines) — a strong payoff as built. |
| 8 | The guard at the hospital — Truman: "Domattina l'ospedale, con il foglio." (`m6.b7b.night.p03`); `jacques_preso`'s "piantonato" note | B9 `m6_news`: Lucy's question "Chi entra ed esce da un ospedale senza farsi notare, agente?" (`m6.b8.news.p03`) and B12 `m6_atto4_bridge`: "qualcuno ha superato un piantone" | **PAID** | The arrest's "piantonato" note and Truman's stated next-day plan | Delete the guard mention → Lucy's question loses its target (a guard nobody was told exists); the breach reads as a generic hospital death instead of a violated safeguard. Neutralize the guard-passed line at the bridge → Act 4's "someone can reach anywhere" hook weakens to "someone killed a witness," a materially smaller claim. | Solid as built; the report's proposed **new** walk (B7, visiting the guard before the news) would strengthen this from told-then-told-again to seen-then-broken — currently the player is only told about the guard, never shown it, before it's already breached. |
| 9 | The hospital register | Report proposes a reader at `m6_atto4_bridge` ("one variant if the register was read," map §1 row 33 and §4 row 5) | **UNPAID** | `m6_hospital` (optional, live) writes `jacques_death_suspicious`/`night_log_no_visitor`; `m6_atto4_bridge` (live) has no `pages_by_value` or condition branching on either flag | Delete the setup (skip `m6_hospital`, optional) → `m6_atto4_bridge`'s text is identical either way, confirming the flag is inert. Neutralize the proposed variant → the register becomes pure flavor, contradicting its own node's stated purpose ("corroborate," report §12 row B10). | Second clean case of the ablation test proving "unpaid": presence or absence of the setup produces no textual difference downstream today. |
| 10 | The Giant's three things and the object "che le apparterrà quando sarà vero" | Act 4 and beyond (explicitly out of this act's scope; report §10: "Both Giant appearances are necessary: the first is the act's end hook, the second is Act 4's turn") | **INTENTIONAL-UNRESOLVED** | `gigante1_dlg` (classic, out of read scope for this ledger) sets `gigante1`, required by `m6_atto4_bridge` | Delete `gigante1` → `m6_atto4_bridge`'s own gate (`conditions: flag gigante1`) blocks the whole act's close, so the setup is load-bearing by construction, not optional. Neutralize the object-promise → Act 4's turn (per report) loses its engine, but that failure belongs to Act 4's ledger, not this one. | Correctly scoped as unresolved-by-design; verify against Act 4's own ledger when written, not here. |
| 11 | S1 custody (ring) — Act 3, `m5_s1` | Report defers explicitly to "the Loggia echo (finale pass)" (map §1 row `s1`; report §6) | **INTENTIONAL-UNRESOLVED** | The custody choice's two feedback branches (`m5.b9.s1.institutional.p02` / `m5.b9.s1.documented.p02`) | Delete the custody choice → nothing later in Act 3 breaks (nothing reads `s1` again inside this act), confirming the deferral claim is accurate, not a cover for an actual unpaid setup. Neutralize the eventual Loggia payoff → the "object Cooper cannot yet read" (report §4) loses its resolution, but that is explicitly out of scope for now. | Distinguish from row 9/6: here the *absence* of any in-act reader is declared and intended, not discovered by ablation. |
| 12 | Hawk's "impronte mie e tue" (A2, `m5.b1.bridge.p04`: "Da qui in poi le impronte sono mie e tue.") | Report §7: "Preserves the scene" is the throughline of Hawk's whole role — paid continuously through A3 ("Tu fai il primo passaggio," `m5.b3.discovery.p02`), A14 (walking the tracks with him), and the dock in `m6_arrest`/ferry scenes | **PAID** | The bridge line itself, then every subsequent Hawk placement that honors "the prints are mine and yours" (he never enters the car, never leads at OEJ) | Delete the bridge line → Hawk's restraint elsewhere (staying outside the car, at the ferry, on the dock) reads as generic caution instead of a stated code being kept. Neutralize by having Hawk enter the car or lead inside OEJ → breaks the promise outright; would be a payoff *reversal*, not a neutral swap. | Diffuse, cumulative payoff (a character-consistency promise, not a single flag) — correctly not tied to any one state variable; matches bible's "quiet-scene carve-out" logic (named effect, no flag required). |
| 13 | The north tracks (old prints "portano a est, nessuna torna indietro," A2) | Report's PROPOSED A14 (north cut, walked) and the `east_route_confirmed` gate on the OEJ door | **PAID, but by the wrong mechanism** — see Fact Ledger violation 2 | The bridge line only; A14 does not exist as a separate walked beat yet | Delete the bridge line → `m5_report_close` still writes `east_route_confirmed` from Hawk's door-side line alone, so the payoff *mechanically* survives ablation of its own setup — which is itself the diagnostic that the "setup" (walking the tracks) is not actually load-bearing yet. Neutralize A14 (i.e., don't build it) → nothing changes from today's build, confirming the walk is currently decorative-if-built, not yet functional. | This is the ledger's version of Fact Ledger violation 2: the payoff (OEJ opens) is currently wired to a line, not to the trace. Building A14 without also re-gating `east_route_confirmed` on it would leave this row unchanged. |

## UNPAID or weak

- **Row 4 (stove):** build the PROPOSED `E_STUFA` node (report's A7) on the existing `traincar` map before `m6_interrogation_falsa` can read it — no new scene; it is one more `mandatory_beat:false` observation node parallel to `m5_scene`, reusing the existing object-interaction pattern. Minimal repair: add the node; leave `m6.b5.falsa.p06` untouched (map §4 row 2 already says "unchanged text").
- **Row 5 (cards):** same repair shape as row 4 — build the PROPOSED `E_CARTE` node (A8) on `traincar`; then add the one-line recall page before `m6_tactic`'s choice (map §4 row 1) and the optional support pair in `m6_p5` (map §4 row 3). No new scenes, three additions to existing nodes.
- **Row 6 (Audrey's promise):** minimal repair is moving one line, not writing one: retire classic `truman_wait4` (already recommended by report §10, "MOVE its Audrey line into the M6 night report as a conditional variant") and insert the report's proposed line as a `pages_by_value`/conditional branch inside `m6_return_night` or `m6_news`, keyed on `audrey_vista_oej`. Text already drafted by the report (§9): *"La ragazza del guardaroba è tornata a casa con la barca delle otto. L'ho accompagnata io."*
- **Row 9 (hospital register):** minimal repair is a conditional branch inside the existing `m6_atto4_bridge` node, keyed on `jacques_death_suspicious`, adding one Truman line (not yet drafted in the reviewed files — report only specifies "one variant," map §4 row 5). No new node required, since `m6_atto4_bridge` already fires after `m6_hospital` would have run.
- **Row 13 (north tracks / east_route_confirmed mechanism):** minimal repair is re-gating, not new content: move the `set: east_route_confirmed` effect from `m5_report_close` to the PROPOSED A14 north-cut node (or add a precondition on `m5_report_close` requiring A14's `node_done`), so the OEJ door's gate reflects a walked trace rather than a spoken line. This directly resolves Fact Ledger violation 2 as well.

## Frozen resolution

| Row | Status after freeze | Where |
|---|---|---|
| 4 stove | PAID (optional) | A7 node; `m6.b5.falsa.p06` unchanged |
| 5 cards | PAID (optional) | A8 node; recall page before `m6_tactic`; optional pair in `m6_p5` |
| 6 Audrey's promise | PAID | `pages_after_branch`-style variant in `m6_return_night` on `audrey_vista_oej`; classic `truman_wait4` retired. Not-met case: silence, by decision |
| 8 guard | PAID, now *seen* | new mandatory B7 `m6_hospital_guard` before `m6_return_night` (O5) |
| 9 register | PAID | Truman variant in `m6_atto4_bridge` on `jacques_death_suspicious` |
| 11 S1 custody | INTENTIONAL-UNRESOLVED (meaning) + PAID (procedure) | R3 line at `m6_atto4_bridge`; Loggia echo still deferred |
| 13 north tracks | PAID by the right mechanism | `east_route_confirmed` written by the north-cut node (O3) |
| 2, 10 | INTENTIONAL-UNRESOLVED | unchanged; Act 4 / finale ledgers |
