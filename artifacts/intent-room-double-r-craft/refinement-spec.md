# Production refinement R — Astra art specification

Lead decision: refine Study A, not cumulative Study D. Use A source `e096ed0:js/retro-authored.js` for `interiorBooth` and its occupied prop offset, then replace booth rectangles with exact construction below. No changes to map, lights, animation, camera, actor pose/gesture/hands, or other art. Coordinates relative `(x,y)`, `w=48`; calls `R(g,x+dx,y+dy,width,height,p.color)` with exclusive right/bottom. Existing palette only. `GAME.CharacterActivity` call, guest origin, table-state branching, table props and hand redraw keep same logic.

1. Backrest silhouette `R(0,-14,48,15,redDark)` and cap `R(2,-15,44,1,woodHi)`. For s=3,17,31, in paint order: `(s+1,-13,11,1,red)`; `(s,-12,13,9,red)`; `(s+12,-11,1,8,redDark)`; `(s+2,-12,7,1,redHi)`; `(s+1,-3,11,1,red)`; `(s+2,-2,10,1,redDark)`. No redLight, wood frame down sides or bright rib texture.
2. Seat `(5,-1,38,6,redDark)` and `(6,-1,36,1,red)`. Left cheek: `(2,-4,3,1,red)`, `(1,-3,5,10,redDark)`, `(2,-3,3,1,redHi)`, `(2,-2,4,7,red)`, `(5,-1,1,7,redDark)`, `(2,5,3,2,red)`, `(2,7,3,2,redDark)`. Mirror on right via `dx'=48-dx-width`. Cheeks before guest; never repaint over head/shoulders.
3. Keep A's guest shadow, `seatedFrame`, `interiorSeatedGuest`, existing gesture, after-table `interiorSeatedHands` and small pose highlights. Paint guest after seat/cheeks.
4. Table before props: `(6,1,36,9,creamShade)`, `(6,1,36,2,cream)`, `(6,10,36,1,woodHi)`, `(7,11,34,1,woodDark)`. No burgundy apron or 48px base. Table remains x+6..41, props supported by it.
5. Raise table props vertically **one pixel** without horizontal change: call `interiorTableProps(g,x+5,y-1,p,variant)` or `interiorOccupiedTable(g,x,y-1,w,p,guest,gesture)`. Retain A's right-seat `propX=x+6` and left-seat x+w-11. Verify every prop/saucer/ring/shadow within tabletop top y+1..9 or local rim as intended; if exact preexisting small prop protrudes, report before altering geometry. Hands remain same.
6. After hands/highlights, bench feet `(2,9,3,7,woodDark)`, `(43,9,3,7,woodDark)`, `(3,9,1,6,woodHi)`, `(44,9,1,6,woodHi)`; table legs `(9,12,2,5,woodDark)`, `(37,12,2,5,woodDark)`, `(10,12,1,4,woodHi)`, `(38,12,1,4,woodHi)`. Local shadow 3px under each bench foot row+16, 2px under each table leg row+17, same A rgba. No continuous shadow/cabinet/stripe. Existing floor visible beneath slab.

Capture same fixed suite as A/B/D under `artifacts/intent-room-double-r-craft/R/`: whole-room populated/native, unpopulated/native, Act4/native; booth-2 occupied, booth-0 ready, booth-3 cleared crops 64×48 native and nearest 4×; manifest with SHA/source, exact commands/dimensions. Before commit run six targeted gates from A, `git diff --check`, compare source diff to `7472a39` to guarantee only booth/prop coordinates. Commit R source+artifacts+spec checkpoint, no push. The lead, not implementer, decides KEEP/REFINE/REVERT.
