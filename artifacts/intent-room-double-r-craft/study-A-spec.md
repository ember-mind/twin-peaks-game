# Study A — exposed seat wings (implementation spec)

Source concept: Astra art study A, interpreted by lead. Change only `interiorBooth` and, if needed, one `interiorOccupiedTable` prop offset in `js/retro-authored.js`. Four existing calls receive same grammar. Use existing diner palette. Pixel rectangles integer; `w=48`, all coordinates relative `(x,y)`.

1. Remove the full-width cabinet slab below booth. Rear backrest mass occupies `x+1..46, y−15..7`; its outer dark border remains. Keep restrained upper wood cap `y−15`, dark burgundy field and **broad** upholstered back panels with vertical separations that end by `y−3`. No seven full-height bright bars and no broad rose fill.
2. Seat top spans `x+3..44, y−2..5`, dark burgundy base plus a thin lit upper edge. Seat front lip `x+3..44, y+6..7` dark burgundy, with one short upper highlight; after table paints, approximately three visible red pixels per side must survive at `x+3..5` and `x+42..44`, rows `y+1..7`.
3. Table is independent: `x+6..41, y+1..10` cream/creamShade flat top with a lighter north 2–3 px edge; wood front rim `x+6..41, y+11..13` with dark 1 px underside. **No full-width red apron**. Keep existing tableware and hands with same believable support. A right-seated guest's left-side table prop may shift +1 px to land fully within new top.
4. Two rear bench feet near x+2..4 and x+43..45 from `y+8..15`; two subtle table supports under the rim near x+9..10 and x+37..38 from `y+13..16`. Leave actual floor visible in the gaps instead of filling x..x+47 at y+14..15. Use short grounded 1–2 px contacts beneath supports, not one solid 48 px shadow.
5. Preserve guest origin, body-before-table, hands-after-table, 4 booth variants and authored table states. No changes to model/collision/depth sorting. Avoid marks outside prior booth envelope `x..x+48, y−16..18`. Do not edit lights or animation.

Capture fixed normal narrative entry `(6,8)` facing up: full native 256×192 and same booth-2 occupied crop `(24,128,64,48)` native/nearest 4×. Also booth-0 ready and booth-3 cleared crops with exact packet crop coordinates, and no-narrative whole-room/Act4 view if straightforward. Put Study A under `artifacts/intent-room-double-r-craft/A/`, include a manifest with commands, image SHA and source revision. Run `node test/diner-layout.js`, `node test/diner-program.js`, `node test/retro-production.js`, `node test/interior-prop-semantics.js`, `node test/smoke.js`, `node test/walkthrough.js`, `git diff --check`. Do not run `double-r-location-native` as a gate; it fails in unchanged baseline. Commit source and study artifacts as one checkpoint, no push.
