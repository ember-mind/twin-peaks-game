# Booth renderer and gameplay boundary — factual packet supplement

- `js/retro-authored.js:4384-4420` draws each 48 px booth in diner-only `drawDinerInterior` (`:4671-4722`). Existing shell spans local `y−15..y+18`; four modules share one helper. Current visual envelope stays within each authored 3-tile booth footprint plus its rear elevation and tight contact rows.
- Palette source `INTERIOR_MATERIALS.diner` (`js/retro-authored.js:4256-4261`): ink `#292b26`; cream `#f4e6c8`, shade `#cfbc92`; red `#8c2f3e`, high `#c45a61`, light `#e28b80`, dark `#501f29`; wood `#5b3a28`, high `#946345`, light `#b88759`, dark `#35271f`; metal `#81918b`, high `#d9dfc9`. Existing contact shadows and practical pools use crisp translucent rows, no gradients or blur.
- Canonical `js/maps.js:229-246`: booths `(1,6,3)`, `(10,6,3)`, `(1,8,3)`, `(10,8,3)`; `h` backrest and `t` table glyphs own collision. Door/spawn from `world/connections.json`: south triggers `(6,9),(7,9)`, spawn `(6,8)` facing up. Those sources remain unchanged.
- Booth paint order: rear shell/seat → generic guest pose → cream tabletop/front/props → guest hands redrawn over tabletop. Overlap functions `interiorSeatedGuest`, `interiorOccupiedTable`, `interiorSeatedHands` (`js/retro-authored.js:4313-4382`). Two generic booth guests belong to `map.interior.guests`, distinct from Named Cast. Empty/ready, occupied and empty/cleared table states share booth geometry.
- Whole diner ground pass draws counter, stools, booths, objects, then actors; foreground pass reintroduces counter/specials by actor foot-depth. Booth painter diner-only. Sprite scale 24 px-derived actors on 16 px tile baseline. Native canvas 256×192; 14×10 map centered at `(16,16)` with no zoom.
- Gameplay-constrained: map rows/footprints, door/spawn, Cast tiles, collision, camera, actor depth. Visual-only: booth rectangles, contact pixels, table/seat treatment, props and local value accents, provided they still visibly agree with collision and guest hands.
- Static booth light strengths `[.75,1.05,.9,.5]`; no changes to previous Ambient/Character Life. Targeted base gates passed: `diner-layout`, `diner-program`, `interior-prop-semantics` 24/24, `character-activity`, `ambient-life`, `ambient-life-frames`, `retro-production` 54/54.
