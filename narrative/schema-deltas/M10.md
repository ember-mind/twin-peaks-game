# Schema-delta M10 — «L'interrogatorio» (Act 5 pass 01)

Sorgente unica: **CONFESSION LOCK v1.1.1** (`M9-M10 v1.1 (confession lock).md`) §4 ordine,
§5 script, §6 dati, §7 test. Ogni pagina di `narrative/missions/M10.json` è una riga di §5
(verificato da `test/narrative-validate-m10.js`, confronto verbatim col file del Lock).

## 0. Riuso (nessun costrutto nuovo)

- **Choice-node con pagine + `completion_when`** (M6 `m6_tactic`, M8 `m8_diner`): `m10_soglia`
  (metodo) e `m10_s3` (nastro). Scelte write-once; il widget si riapre finché il valore non esiste.
- **`next` / continuation** (M5 rapporto→S1, M8 `m8_discovery`): la scena è UNA catena
  `m10_soglia → … → m10_morte`; un'interruzione riprende dal primo anello non risolto.
- **`exposed: false`** (runtime `worldRoots`): gli anelli 2–9 non sono root. Root esposte:
  `m10_soglia` su Leland (fino al fermo) e `m10_morte` su Truman (dopo il fermo Leland è in cella,
  nessun corpo: la ripresa passa da Truman — V5b).
- **`pages_by_value`** su `m10_method` (una sola per nodo): `m10_domande`, `m10_confessione`.
- **`conditional_pages`** (C8-B): eco `promise_stance`, `JACQUES_*`, biglietto (warning_target ×
  focus_destination).
- **`feedback_pages`** sulle scelte S3.
- **Portrait di pagina** (`portrait: "bob"`, già usato dal finale) per i due segnali di BOB.

## 1. `material_admissions` — un record per fatto (Lock §6)

Sei valori tipizzati `material_admissions.<fatto>` (taxi_lie, traincar_presence, laura_homicide,
maddy_homicide, maddy_body_transport, letters), dominio `speaker_register`
(`leland_first_person | voice`, già a catalogo). `recorded` = `value_set`; `speaker_register` = il
valore. **Mai un booleano.** Unico writer: `m10_confessione` (effetti committati dopo l'ultima
pagina, identici nei tre metodi). S3 (`m10_s3`) è gated sui sei `value_set`.
Diff: `diff-state-enums-M10.json` (values_allowed + `m10_method`, `s3`, `truman_testimony_state`).

## 2. `factual_status` di proposizione — UNA estensione di runtime (argomentata)

Il Lock §5-B6 chiede `P6: confirmed_as_lie; P8: corroborated`. Il runtime scriveva solo
`formulation.status`; il finale retro lo faceva a mano fuori dal runtime
(`commitAdmissionsToNarrative` in `js/narrative-finale-production.js`). Pressione ripetuta
(propositions.json dichiara `factual_path` per P6 e `factual_ceiling` per P8 dal pacchetto v1.0).

```json
{ "proposition": "P6", "factual_status": "confirmed_as_lie" }
```

Regole (`js/narrative-runtime.js`): validato in prepare (dominio `factual_status`; proposizione
già `formulated`; transizione solo da `unconfirmed` o idempotente sullo stesso valore), applicato
in commit. Generico: nessun `if (mission)`. Classe B (estende la chiave `proposition` esistente),
non una primitiva nuova di scena.

## 3. Metadati di pagina/nodo (inerti per il runtime, letti dai test)

`bob_surface` (`portrait_shown | register_shift`, Lock §6: due segnali), `admits` (fatti resi da
quella pagina), `recorded_on` (`personal_recorder | tape`), `tape_opening: neutral`,
`killer_only_bridge`, `cast_exit` / `cast_entry`, `laura_photo`, `lock_composite` (pagina
composta da due pezzi della stessa riga del Lock). Nodo `m10_post_s3`: `spoken_in_all_paths`,
`recorded_if`, `witnessed_by_truman`, `formal_testimony_if` (Lock §5-B7b).

## 4. Ingresso, uscita, ownership

- Ingresso: `{ all: [ {flag: atto5}, {node_done: m9_arrivo} ] }` = consegna di M9.
- Uscita: `completion.when = { flag: leland_morto }` (unico writer mission: `m10_morte`).
- Obiettivi: `obj_m10_1` «Leland Palmer è alla centrale. Decidete come parlargli.» fino a
  `leland_morto`; `obj_m10_2` «Torna alla Loggia (Glastonbury Grove).».
- Cast (`narrative/cast/windows.json`): `ACT5_LELAND_STATION` esteso (esce a `m10_fermo`,
  `exit_authored_by m10.b9.fermo.p03`), nuovi `ACT5_LELAND_CELL` (OFFSCREEN cell) e
  `ACT5_TRUMAN_OUT_INTUITIVE` (OFFSCREEN corridor, pagine autorali di uscita e rientro).
- **Il finale retro (`js/narrative-finale.js`) non possiede più M10**: viene armato a
  `leland_morto` e parte dalla Loggia (vedi report Act 5 pass 01, fase 3).

## 5. Che cosa NON cambia

Transazioni prepare/commit, write-once, save schema 1.1.0, M4/M5/M6/M8. M9 cambia solo per le due
derive corrette in fase 1. Nessuna porta, nessuna mappa, nessun asset nuovo: la stanza e la cella
sono rese dalle pagine del Lock sulla mappa `sheriff` esistente.
